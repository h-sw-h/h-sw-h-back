name: 🚀 CI/CD 파이프라인 - EC2 자동 배포

on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: image_hub
  IMAGE_TAG: ${{ github.sha }}
  IMAGE_LATEST: latest

jobs:
  build_and_push:
    name: 📦 Github Repository → ECR 이미지 빌드 & 푸시
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 코드 불러오기
        uses: actions/checkout@v4

      - name: 🐳 Docker Buildx 설정
        uses: docker/setup-buildx-action@v3

      - name: 🔐 AWS 로그인
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 🔑 ECR 로그인
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: 🏗️ Docker 이미지 빌드 & 푸시
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          docker build -t $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }} .
          docker tag $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }} \
                     $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_LATEST }}
          docker push $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
          docker push $ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_LATEST }}

  deploy_ec2:
    name: 🌐 EC2 서버에 Docker Compose 배포
    runs-on: ubuntu-latest
    needs: build_and_push
    steps:
      - name: 🧩 코드 불러오기
        uses: actions/checkout@v4

      - name: 🔌 EC2 접속 & 배포 실행
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            echo "🔁 최신 Docker 이미지 Pull 중..."
            aws ecr get-login-password --region ${{ env.AWS_REGION }} \
              | sudo docker login --username AWS --password-stdin \
                ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com
            cd /home/${{ secrets.EC2_USER }}/apps/hf-back

            # ✅ .env 파일 자동 생성 (GitHub Secrets 값 주입)
            cat <<EOF > .env
            APP_NAME=FastAPI Application
            DEBUG=True
            HOST=0.0.0.0
            PORT=8000

            # OpenAI API Key
            OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
            EOF

            sudo docker compose pull
            sudo docker compose up -d
            sudo docker image prune -af
            echo "✅ 배포 완료!"